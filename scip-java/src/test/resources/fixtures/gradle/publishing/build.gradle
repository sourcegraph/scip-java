plugins {
    id 'java'
    id 'maven-publish'
}
repositories {
    mavenCentral()
}
publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'org.scip-code'
            artifactId = 'example-library'
            version = '1.1'
        }
    }
}
