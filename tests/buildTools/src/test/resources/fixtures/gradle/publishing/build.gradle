plugins {
    id 'java'
    id 'maven-publish'
}
repositories {
    mavenCentral()
}
publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.sourcegraph'
            artifactId = 'example-library'
            version = '1.1'
        }
    }
}
